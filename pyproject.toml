
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sagittal-brain"
version = "0.1.0"
dependencies = [
  "numpy",
]

[tool.hatch.build.targets.wheel]
include = [
  "sagittal_brain.py",
]
